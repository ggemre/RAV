import stdlib/sysio.rav

fn main() {
	int x = 5+3/ 2;
	print.call(x);

	int msg = "Hello world!";
	print.call(msg);

	int x = 0;
	while (x <= 10) {
		if (x % 2 == 0) {
			print.call(x);
		} else {
			print.call(0);
		}
		x = x + 1;
	}
}